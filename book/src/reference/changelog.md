# Changelog

This file is a synced copy of the root-level `CHANGELOG.md`. In CI
the `docs.yml` and `tests.yml` workflows run
`cp CHANGELOG.md book/src/reference/changelog.md` before invoking
`mdbook build book`, guaranteeing the two never drift.

The version committed to git here is a placeholder — CI overwrites
it before the site is built.
